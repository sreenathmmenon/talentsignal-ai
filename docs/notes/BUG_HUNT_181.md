# Exhaustive Bug Hunt — 181 verified real bugs

Total: 181 | Hackathon: 20 | Severity: {'critical': 4, 'high': 32, 'medium': 67, 'low': 78}

# Consolidated Verified Bug Report — 181 real bugs (20 in HACKATHON area, 161 elsewhere)

Honest note: 181 distinct real bugs after deduping 7 cross-area near-duplicates. This is UNDER the 200 target — no fake bugs were added to pad the count.

## THE HACKATHON CHALLENGE (20) — 1 critical · 5 medium · 14 low

### critical
1. submission_metadata.yaml:17 + README.md:130-133 · critical · README reproduce block runs spine (no --engine flag) but committed final_submission.csv is the hybrid output; hybrid reproduces it byte-for-byte, spine yields a totally different top-100. · Put `--engine hybrid --index-dir outputs/index` in the README reproduce block (or commit the spine output).

### medium
2. submission_spec.docx §2 vs outputs/final_submission.csv · medium · Spec requires upload filename = registered participant ID (team_xxx.csv) under auto-rejection; pipeline writes final_submission.csv and the local validator only checks a non-empty stem. · Document the rename to <participant_id>.csv or write outputs/<participant_id>.csv.
3. ranking.py:80-90 + scoring.py:440 + semantic_match.py:147-169 · medium · If a candidate id is missing from the index, the dense channel silently turns off (lexical-only) yet ScoreBreakdown.engine stays 'hybrid' with no warning; any id drift between candidates.jsonl and the index silently degrades hybrid to lexical. · Track index coverage and label engine='hybrid(lexical-fallback)' / warn when embeddings are missing.
4. README.md:111 · medium · '5,400 candidates rank in ~0.45s' is inflated; measured 1.59s for 5,400 and 34.85s for 100K (not the ~8.3s a linear 0.45s/5.4k implies). · Replace with measured numbers (~1.6s/5.4k, ~35s/100k).
5. deck/TalentSignal_Deck.html:234 · medium · Deck headlines '~30s rank time' unlabeled, but the submitted CSV is the hybrid engine whose measured 100K rank is 64.33s (>2x). · Label deck timing by engine (spine ~35s, hybrid ~64s).
6. .gitignore:16 + Dockerfile:26-28 · medium · Dockerfile CMD hardcodes the candidates.jsonl path, .gitignore excludes it, no .dockerignore; bare `docker run` quickstart fails with a raw FileNotFoundError on a fresh clone. · Exit with a clear 'dataset not found — mount it' message and document the volume mount.

### low
7. outputs/eval/METRICS.md:42 · low · METRICS lists spine 100K rank as ~30s; measured 34.85s, so the single-source-of-truth modestly understates typical spine time. · Update to ~35s or note machine variance.
8. features.py:10 + consistency_audit.py:29-30 + schema_profile.py:133 · low · Reference dates are hardcoded and inconsistent (features date(2026,6,14) day-level; audit/schema month-level 2026-06), anchoring recency to a frozen stale date. · Derive one shared reference date from date.today() and pass it through.
9. consistency_audit.py:200-205 · low · _date_before gates on a strict zero-padded YYYY-MM-DD regex then does a raw string compare; unpadded dates like 2026-1-5 fail the regex so an end<start contradiction is never flagged (no occurrences in the real 100K). · Parse to datetime.date and compare.
10. scoring.py:127 & 219 · low · Preferred-city detection uses substring containment, so 'pune' matches inside 'puneet vihar', awarding the +0.25 logistics bonus to candidates not in a preferred city. · Match on the location's city token, not substring.
11. rank.py:102 · low · `--rerank` 'retrieve a larger shortlist' is a no-op: retrieve_n = max(100,60) == 100 in both branches, so rerank only reorders the same top 60 and can never promote ranks 61-100. · Actually widen retrieval (e.g. 300) or fix the misleading help.
12. rank.py:30-76 (_rerank_rows) · low · The --rerank path can never run in the declared rank env: requirements.txt installs only numpy, the reranker needs sentence-transformers, so available() is False and rows pass through unchanged. · Document that --rerank needs the precompute env, or ship the cross-encoder offline.
13. live_cache.py:85 · low · Reads the 100K dataset with `open(_DATASET)` (no encoding, no with-block) in a list comprehension; handle closes only at GC and can mis-decode on non-UTF-8 systems; inconsistent with io.iter_candidates (forces utf-8). · Use `open(_DATASET, encoding='utf-8')` in a with-block or reuse io.iter_candidates.
14. scoring.py:115-122 · low · Seniority only enters the low-weight quality factor, so the committed top-100 for 'Senior AI Engineer 5-9 years' contains 5 'Junior ML Engineer' titles (four are 6+ yrs, ranked 44-81, none top-10). · Add a stronger seniority gate / relevance-independent floor for roles with an explicit senior band.
15. reasoning.py:180-181 · low · The 'Open to work, X% response rate' clause is emitted with no staleness check, so a candidate flagged open_to_work but last active months ago is still advertised as 'Open to work'. · Gate the clause on recent activity (e.g. last_active_months <= 3).
16. consistency_audit.py:91-98 · low · audit_candidate iterates `for j in career` calling j.get(...) with no isinstance(dict) guard; a bare-string career_history element raises AttributeError inside the auditor (no occurrences in the real 100K). · Filter career to dict entries at the top of audit_candidate.
17. submission_metadata.yaml:31-32 · low · Declares pre_computation_time_minutes: 9 with no reproducible measurement; the committed index means the figure is never re-validated and CPU MiniLM embedding of 100K can exceed it. · Measure and cite the actual precompute wall-clock + machine spec in METRICS.md.
18. .gitattributes:5 + outputs/index/candidate_ids.json · low · git-lfs is configured only for *.npy; candidate_ids.json/meta.json are plain blobs with no checksum binding id order to embeddings.npy row order, so a desync would silently misalign every vector. · Store a content hash in meta.json binding id order to the matrix and validate on load.
19. scoring.py:307 & 415 · low · The 'penalty' column overloads two scales — a fractional soft penalty in [0,0.6] and a sentinel 1.0 for hard-vetoed rows — making factor_scores.csv ambiguous (though vetoed rows rarely appear). · Use a separate boolean 'hard_vetoed' column.
20. live_cache.py:187-194 vs 111 · low · warm() defaults engine='spine'/top_n=10 while rank_live() defaults engine='hybrid'/top_n=20, and the cache key includes both, so a caller relying on rank_live's defaults gets a guaranteed cache miss and a ~64s re-rank. · Make warm() and rank_live() share one default engine/top_n.

## ENGINE (31) — 1 critical · 6 high · 14 medium · 10 low

### critical
21. reranker.py:78-84,101-115 · critical · Head items whose candidate_id is missing from id_to_candidate are skipped and never re-added; output = reranked(valid)+tail, silently dropping head candidates and corrupting ranks/length. · Append unscored head items back into the output.

### high
22. scoring.py:39 (_term_coverage) + semantic_match.py:87/90 · high · The len(w)>=3 salient-word filter drops every 1-2 char skill term (go, c, r, ml, ai, qa, js, bi); a JD must_have=('go',) yields coverage 0.0 for a perfect Go engineer. · Lower the threshold and special-case short tech tokens; align with the semantic_match tokenizer.
23. scoring.py:53 (_term_coverage return total/len(terms)) · high · Denominator counts terms whose every sub-word was dropped (<3 chars), depressing coverage of surviving terms; _term_coverage('python expert',('python','go','c'))=0.333 instead of 1.0. · Divide by the count of terms that contributed a non-empty word list.
24. scoring.py:39 vs 20 / semantic_match.py:87 · high · Requirement split keeps '.' inside the term but the tokenizer regex [a-z0-9+#]+ has no dot, so 'node.js', '.net', 'asp.net' can never match. · Split requirement terms with the same token regex or strip dots on both sides.
25. scoring.py:96-105 (_quality_blend) · high · When all four generic weight keys are absent/zero, total falls back to 1.0 but the numerator is 0, so Q collapses to 0.0 and every candidate scores only at QUALITY_FLOOR (only with a custom/partial weights map). · Fall back to an equal-weight average of the four factors when the generic weights are all absent.
26. features.py:21-29 (months_since max(0,...)) · high · max(0,...) clamps a future last_active_date to 0 months, giving a future/garbage date (e.g. 2030) full activity credit (active_score=1.0). · Treat future dates as invalid (large value / flag) rather than 0.
27. scoring.py:283-296 (consistency import-in-loop + bare except) · high · audit_candidate is imported and run per candidate inside `except Exception: pass`, so a systematically-failing auditor silently disables the honeypot veto for the whole run with no signal; reproduced with a string career entry scoring clean. · Log/raise on unexpected auditor failure; don't treat every exception as 'no inconsistency'.

### medium
28. reranker.py:92-97 · medium · CE min-max is computed over only the shortlist's scores, so the worst shortlisted candidate always maps to 0 and the best to 1 regardless of absolute relevance; an outlier compresses everyone toward 0. · Use absolute/sigmoid calibration of CE logits.
29. scoring.py:120 & 213 (seniority partial-credit band 4..11) · medium · Hardcoded 4 and 11 are JD-independent: a 5-yr candidate hits 0.62 for a senior role (preferred_min=12) and preferred_max>11 leaves an empty upper cushion causing a cliff to 0.25. · Derive the partial-credit band from the JD's own preferred range.
30. scoring.py:280-281 vs 378-384 · medium · The same disqualifier notion is handled oppositely: the spine path adds only a +0.06 soft penalty (gated, floored at 0.6 multiplier) while the hybrid path hard-vetoes to 0.0 (paths also use different lexical vs semantic signals). · Unify disqualifier semantics across both engines.
31. semantic_match.py:106-128 (_COS_CEIL=0.65 clamp to 1.0) · medium · Raw cosine above 0.65 is clamped to exactly 1.0, so all strong matches (0.70/0.90/0.95) collapse to dense=1.0, destroying ordering among the best fits. · Raise the ceil or use soft (sigmoid) compression.
32. scoring.py:112,297,411 (soft_multiplier 0.6 floor) · medium · soft_multiplier is clamped with a 0.6 floor, so no accumulation of soft penalties can reduce a score below 60%; a profile with many serious-but-soft issues still keeps 0.6x. · Lower/remove the floor or escalate severe cumulative soft penalties to a veto.
33. scoring.py:204 (substring 'ai' in current_industry) · medium · 'ai' in ev.current_industry matches Retail/Air-travel/Bridal etc. (and 'hr' similarly), granting a +0.10 career bonus to off-role candidates. · Match whole words / a curated industry set.
34. features.py:185 (skill_assessment_max float cast) · medium · max([float(v) ...]) crashes on a non-numeric assessment value, aborting build_evidence for that candidate and the whole run; reproduced with {'python':'high'}. · Coerce defensively, skipping non-numeric values.
35. features.py:135 (int(skill.get('duration_months') or 0)) · medium · int(...) crashes on a non-integer string like '6 months' or '1.5', killing build_evidence. · Use a tolerant numeric parse (try/except or float-then-int).
36. semantic_match.py:147-150 (use_dense length guard) · medium · The dense channel is disabled for the WHOLE JD whenever len(req_embeddings)!=len(requirements), silently degrading all semantic matching to lexical-only with no warning. · Skip only the mismatched requirement and surface a loud warning.
37. scoring.py:267-271 (no-must-have relevance floor 0.4, spine) · medium · When job.must_have is empty, relevance is floored at 0.4 for every candidate, so even off-topic profiles clear top10_eligible (>=0.34). · Require title/nice evidence before top-10 eligibility instead of a blanket floor.
38. semantic_match.py:196-198 (semantic_fit 0.85/0.15 split) · medium · semantic_fit=0.85*must+0.15*nice; with must-haves but no nice-to-haves, nice_fit=0 caps semantic_fit at 0.85 even for a perfect candidate, structurally penalizing JDs without nice-to-haves. · Renormalize the split by which buckets actually exist.
39. scoring.py:170,280 (disqualifier_coverage single-word binary over all_text) · medium · A single-word disqualifier like 'intern' present anywhere (incl. ancient history) gives binary full credit and triggers the soft penalty for a now-senior candidate. · Scope disqualifier matching to current/recent evidence or require multi-signal confirmation.
40. scoring.py:347-348,401-408 (hybrid empty-requirements floor) · medium · With requirements empty/unparsed, semantic_fit/coverage=0 and relevance is floored at 0.4 for everyone, so a JD with zero parsed requirements ranks purely on generic quality while still reporting engine=hybrid relevance. · Guard empty requirements explicitly and surface low confidence.
41. semantic_match.py:120-128 (cosine NaN/Inf) · medium · cosine guards zero-norm but not NaN/Inf; a NaN row returns 1.0 via min/max and propagates NaN into final_score, breaking sort ordering (needs corrupt embeddings). · np.nan_to_num inputs or return 0.0 when not np.isfinite.

### low
42. reranker.py:100 vs ranking.py:27,90 · low · rerank's sort has no secondary tiebreaker while the retrieval path sorts with an id tiebreak, so the two paths can disagree on ties (each run is still reproducible). · Add candidate_id as a stable secondary sort key.
43. ranking.py:103 (_rows_from_scored display_score) · low · display_score=max(0,final-(rank-1)*1e-6) invents a fake 1e-6 gap for genuine ties and shaves ~9.9e-5 off rank 100, so the displayed number no longer equals final_score. · Display the true rounded final_score and enforce ordering via sort/tiebreak only.
44. features.py:29 (day-of-month correction) · low · The year*12+month heuristic mis-buckets near-boundary dates: 2026-05-20 returns 0 months (active-today) while 2026-05-10 returns 1. · Compute an exact day difference //30 or use relativedelta.
45. scoring.py:16-20 (_tokenize lru_cache on per-candidate text) · low · @lru_cache(4096) keyed on full candidate text thrashes on a large pool (unique keys, ~0 hit rate) while pinning up to 4096 large frozensets. · Only cache the small fixed JD-term tokenizations.
46. scoring.py:253 & 279 (risk_penalty called twice) · low · risk_penalty(flags) is computed at line 253 (dead, overwritten at 307) and again at 279, wasting work and inviting divergence. · Compute risk_penalty once and reuse.
47. features.py:163 & 127 (service_only with blank companies) · low · A blank/normalized-empty company is in the companies denominator but never in service_count, so an all-service career with one blank company is not flagged and the service penalty is skipped. · Exclude blank companies from both service_count and len(companies).
48. category_taxonomy.py:96-103 (validate_weights) · low · Validates only presence and ~1.0 sum; a negative technical_evidence weight summing to ~1.0 passes and flows into scoring/display. · Reject any weight < 0.
49. semantic_match.py:70 (best_evidence_span tie-break) · low · Tie-break prefers the SHORTER evidence sentence among equal keyword hits, surfacing terse fragments as the grounding quote instead of the richer sentence. · Prefer higher keyword density or a sane length window.
50. ranking.py:27,90 (sort key string candidate_id) · low · candidate_id is a string, so numeric ids sort lexicographically ('10' before '2') and a mixed int/str pool would raise TypeError (build_evidence forces str, so the lexical surprise is the live effect). · Normalize id type and sort numerically when numeric.
51. semantic_match.py:170 (kind default 'must_have') · low · Unknown/missing kind defaults to must_have (getattr default + else branch), so a malformed/typo'd kind is counted as a must-have, distorting coverage (needs a hand-built Requirement). · Validate kind against the known set and skip/raise on unknown.

## JD ANALYSIS (35) — 12 high · 14 medium · 9 low

### high
52. jd_ingest.py:105 (_keywords) · high · Tokens shorter than 3 chars are dropped, so short real skills Go, R, ML, AI, UX, BI are lost as keywords. · Whitelist known short tech tokens or exempt tokens containing +/#.
53. jd_ingest.py:104 (strip('.-/') + <3 filter) · high · strip + length filter removes C# (len 2 after strip) and turns .NET into 'net'. · Don't strip '#'/'+'; exempt known short languages.
54. jd_ingest.py:159 (_split_requirement_list on bare '/') · high · Splitting on '/' shatters atomic tokens — CI/CD entirely lost, TCP/IP and A/B fragmented. · Remove '/' from the split set or only split on ' / ' surrounded by spaces.
55. jd_ingest.py:168-185 (_is_title_or_intro) · high · Any line <=6 words with no requirement verb is classified as title and skipped, so short verb-less requirements (Kubernetes and Docker, Fluent Spanish) in the first 4 sentences are dropped. · Require a role-word match or absence of any known-skill token before declaring a line a title.
56. jd_ingest.py:38-41 (NICE_CUES substring) · high · Substring cue matching: 'plus' matches inside 'surplus', mis-tagging a must-have sentence as nice_to_have and halving its weight. · Match cues on word boundaries; drop 'preferred' or require header context.
57. jd_ingest.py:214-222 (_extract_years) · high · Only 'X-Y years' and 'X+ years' match; a plain '5 years of experience' returns (None,None) so seniority falls back to the hardcoded 5-9 default. · Add a lone '(\\d+)\\s*years?' minimum regex.
58. jd_ingest.py:283 (pkey 80-char dedup) · high · The dedup key truncates to 80 chars, so two distinct long requirements sharing an 80-char prefix collide and the second is dropped. · Use the full normalized text (or a hash) as the dedup key.
59. jd_ingest.py:288 (len(pkws)<1 drops disqualifiers) · high · A disqualifier item whose words are all stopwords ('no production') yields zero keywords and is silently dropped. · Keep disqualifier text even with zero keywords, or remove 'production' from stopwords.
60. jd_ingest.py:58-64 (_STOPWORDS) · high · Domain-meaningful words (experience, production, work, users, real) are stopworded, so 'production experience with real users' loses all salient tokens. · Remove those words from _STOPWORDS.
61. jd_ingest.py:66 (_SENT_SPLIT on bare newline) · high · A requirement hard-wrapped across lines is split into fragments; only the first passes the keyword filter and the actual skill body is lost. · Reflow paragraphs before sentence splitting.
62. jd_parser.py:152-157 (strongest band math) · high · With only a min ('10+ years'), pmax falls back to 9.0 while pmin=10.0, producing an inverted/impossible band fed to scoring. · When only a min is parsed, set pmax=max(pmin+buffer, default) and clamp min<=max.
63. jd_ingest.py:51-55 (CITY_GAZETTEER India-only) · high · The gazetteer is India-only, so a non-Indian JD ('San Francisco, London, Berlin') extracts no locations and falls back to Indian default cities, contradicting the JD. · Expand the gazetteer / detect 'in <City>' patterns; don't default to Indian cities for foreign JDs.

### medium
64. jd_ingest.py:159 (split on \\band\\b/\\bor\\b) · medium · Splits on 'and'/'or' inside compound concepts ('command and control'), fragmenting one coherent requirement. · Only split on commas; treat and/or as separators only adjacent to commas.
65. jd_ingest.py:163 (2-char tokens in split) · medium · A genuine list of 2-char skills ('ML, AI, UX, and BI') is not split because each part has zero keywords, staying as one mega-requirement. · Count a non-stopword 2-char token as a valid part keyword for splitting.
66. jd_ingest.py:67-68 (requires literal 'years') · medium · 'yrs'/'yr' abbreviations don't match, so '5-9 yrs' yields no band. · Change 'years' to 'years?|yrs?' in both year regexes.
67. jd_ingest.py:67 (_YEARS_RE char class [-–to]) · medium · The separator is a character class not alternation, so it matches '5oo9 years' (false positive) and rejects '5 through 9 years'. · Use explicit alternation (?:\\s*(?:-|–|to)\\s*).
68. jd_ingest.py:277-278 (emphasis 'expect' substring) · medium · 'expect' in EMPHASIS_CUES matches 'unexpected', spuriously boosting a must-have; emphasis is also gated to MUST_HAVE so emphasized nice-to-haves get no boost. · Word-boundary match EMPHASIS_CUES; decide deliberately whether nice items can be emphasized.
69. jd_ingest.py:126-128 (_is_header_only requires ':') · medium · Headers without a colon ('Things you absolutely need') aren't recognized and leak in as scored requirements. · Treat a short cue-only line with no skill keyword as a header even without a colon.
70. jd_ingest.py:225-230 (city aliases) · medium · Gurgaon/Gurugram and Bangalore/Bengaluru are separate entries, so 'Gurgaon (Gurugram)' yields both as distinct preferred locations. · Canonicalize aliases before dedup.
71. jd_ingest.py:233-245 (non-English JD) · medium · Cue lexicon, year regex and gazetteer are English/India-only, so a Spanish JD extracts no years, defaults everything to MUST_HAVE, with no warning. · Detect non-English and surface a low-confidence flag.
72. jd_ingest.py:236-241 (title no length cap) · medium · For a single-line JD the title becomes the entire multi-sentence blob. · Cap the title to the first sentence and/or ~80 chars.
73. jd_ingest.py:257 (current_kind flip) · medium · A colon-less disqualifier header isn't recognized, so following bare bullets inherit MUST_HAVE, mislabeling disqualifiers as must-haves. · Make section state robust to colon-less headers.
74. jd_parser.py:52-84 (no tab support) · medium · Indent counts leading spaces only, so tab-indented YAML flattens nesting and load_job_spec then raises KeyError. · Reject tabs or expand them to spaces before measuring indent.
75. jd_parser.py:74-83 (next_is_list lookahead) · medium · Container type is decided from the first following child line only; a block that starts with a mapping line then has '- ' items raises 'Unexpected list item'. · Scan all immediate children to decide container type.
76. jd_parser.py:89-107 (load_job_spec direct key access) · medium · Required keys are accessed with no guard, so a spec missing a field raises a bare KeyError with no field/file context. · Validate required keys up front and raise a descriptive ValueError.
77. jd_parser.py:119-129 (YAML path drops emphasis boost) · medium · The YAML path assigns every must_have weight 1.0 while free-text ingest applies a 1.25 boost, so the same role authored two ways gets different weights. · Apply consistent weighting in both paths.

### low
78. jd_ingest.py:67 (_YEARS_RE \\d{1,2}) · low · The years regex caps at 2 digits, so '100 years' returns (None,None) (very contrived). · Use \\d{1,3} if desired.
79. jd_parser.py:35-44 (_parse_scalar underscores) · low · int() accepts underscores, so '1_000' parses to 1000. · Reject values containing '_' before numeric coercion.
80. jd_parser.py:39-44 (scientific notation) · low · '1e9' has no '.', so int('1e9') raises and falls through to the raw string. · Try float() before int().
81. jd_parser.py:37-38 (bool lowercase only) · low · Only 'true'/'false' are treated as bools; 'True'/'False'/'yes'/'no' become strings. · Case-insensitive bool match and support yes/no.
82. jd_parser.py:68 (nested map in list item) · low · A list item containing a colon ('- role: lead') is stored as the literal string, not a nested map. · Document the limitation or parse nested maps in list items.
83. jd_parser.py:103 (preferred_locations tuple()) · low · If 'preferred' is mis-detected as a dict/empty, tuple() yields keys or empty, silently producing wrong locations (needs malformed spec). · Assert loc['preferred'] is a list and error otherwise.
84. jd_ingest.py:157 (_LEAD_CUE strips leading strong/deep) · low · A leading 'strong'/'deep'/'hands-on' is removed; if those words are the list content, the first item is eaten ('strong, deep, hands-on' -> ['deep','hands-on']). · Only strip a leading cue when followed by in/with/of or a separator.
85. jd_ingest.py:101 (token regex first-char [a-zA-Z]) · low · The keyword regex requires a leading letter, so digit-led tokens (3D, 2FA, 5G, 802.11) yield no keyword. · Allow an optional leading digit.
86. eval/jd_library.py:135-140 (spec_path relative) · low · JDEntry.spec_path values are relative and resolved against CWD, so running from another directory raises FileNotFoundError. · Resolve spec_path against a known project-root constant.

## RESUME EXTRACTION / INGEST (32) — 5 high · 14 medium · 13 low

### high
87. ingest/model.py:116-121 (normalize_record) · high · The coerce loop does `if 'duration_months' in s` on a possibly-str s/j (substring test) then s.get(), crashing AttributeError on a string containing that literal substring. · Guard with isinstance(s/j, dict) before the membership test and .get.
88. ingest/adapters.py:32-42 (json_adapter) · high · Single-object detection uses len(lines)==1, so a pretty-printed multi-line single JSON object falls into the JSONL branch and json.loads each line -> JSONDecodeError. · If text starts with '{', try json.loads(whole text) first.
89. ingest/model.py:91-95 + adapters.py:38-39 · high · normalize_record blindly calls rec.setdefault, so non-dict JSON elements ([1,2,3], null) crash with AttributeError. · Guard `if not isinstance(rec, dict)` before normalize_record.
90. ingest/adapters.py:62-83 (csv_adapter BOM) · high · DictReader doesn't strip a UTF-8 BOM from the first header, so the name column key becomes '﻿name', misses _CSV_MAP, and the name is ignored — hits Excel-exported CSVs. · Decode with utf-8-sig or strip the BOM from the header.
91. ingest/model.py:45-51 (_gen_id mod 10^7) · high · candidate_id = sha256 % 10,000,000 gives only 10M ids; at 100K candidates birthday-collision is ~39%, so distinct candidates can share an id and overwrite downstream (affects ingest-generated ids). · Use the full hash (first 12 hex chars) and detect collisions.

### medium
92. ingest/adapters.py:62-65 (TSV inline) · medium · Inline TSV content can't be delimiter-detected (delim chosen by .tsv suffix), so TSV strings are parsed with comma and all fields are lost. · Sniff the delimiter from the header line.
93. ingest/adapters.py:55-56,75 (_CSV_MAP 'experience') · medium · 'experience' aliases to years_of_experience and runs through _num(), so a free-text cell ('Worked at Google for 8 years') yields the first number as years. · Don't alias ambiguous 'experience' to a numeric field, or only accept purely-numeric cells.
94. ingest/resume_parser.py:46,72-75 (_PHONE / name) · medium · _PHONE matches any long digit run incl. date ranges, and the name loop skips _PHONE-matching lines, so a name on the same line as '2015 - 2019' is dropped. · Require '+'/grouping/length checks that exclude bare year ranges.
95. ingest/resume_parser.py:47,93-95 (_YEARS_EXP) · medium · years_of_experience takes the FIRST 'N years' match, so 'Worked for 2 years... Total 12 years experience' yields 2.0 instead of 12. · Take max of all matches or prefer 'total experience' phrasing.
96. ingest/resume_parser.py:136-150 (_CITIES[:14] includes 'Remote') · medium · 'Remote' at index 13 is force-assigned country='India'. · Drop 'Remote' from the India-city slice.
97. ingest/adapters.py:145 (linkedin name) · medium · name = First + ' ' + Last with no guard yields ' ' when both are absent, which is truthy and survives `c.name or 'Candidate'`. · name = ' '.join(filter(None,[first,last])).strip().
98. ingest/registry.py:35-60 + adapters.py:167-176 · medium · A bare JSON string with no extension is detected as 'text' and shredded by the resume parser instead of parsed as JSON. · In detect_format sniff a leading '{'/'[' and route to json.
99. ingest/model.py:51 (canonical_record seed) · medium · The id seed is name+summary+company+raw_text[:200]; for CSV/LinkedIn raw_text='' so two same-name people with blank fields collide deterministically and overwrite. · Include skills/career/full raw_text in the seed or fall back to a uuid.
100. ingest/adapters.py:100-110 (pdf_adapter) · medium · No file-size/page-count guardrails and per-page extract_text isn't wrapped in try/except, so one corrupt page fails the whole file. · Cap page count/file size; wrap per-page extraction.
101. ingest/adapters.py:115-125 (docx_adapter) · medium · Only document.paragraphs is read — text in tables/headers/footers/text boxes is silently dropped (common resume layouts). · Also iterate document.tables and headers/footers; add archive size limits.
102. ingest/adapters.py:167-176 (_read_text errors='ignore') · medium · errors='ignore' silently drops undecodable bytes (cp1252/latin-1 smart quotes, accented names: 'José Müller' -> 'Jos Mller'). · Try utf-8 then latin-1, or at least errors='replace'.
103. ingest/adapters.py:167-176 (_read_text missing path) · medium · Falls back to str(source) when the path doesn't exist, treating a missing/typo'd file path as inline content and parsing it as a resume. · Raise FileNotFoundError when source looks like a real path with a file extension but is absent.
104. ingest/resume_parser.py:289-305 (_parse_education) · medium · Fabricates start_year=2014/end_year=2018 when no year is found and assumes a fixed 4-year degree when one is found. · Leave start/end year None when unknown instead of fabricating.
105. io.py:22-23 (iter_candidates missing id) · medium · A record lacking candidate_id raises ValueError mid-stream, aborting the whole file after earlier rows were yielded -> total loss of remaining valid rows. · Collect/skip-and-report bad lines or validate the whole file before yielding.

### low
106. ingest/adapters.py:64 (TSV case-sensitive) · low · TSV detection str(source).endswith('.tsv') is case-sensitive, so data.TSV is comma-split. · Lowercase before comparing.
107. ingest/adapters.py:67-72 (duplicate headers) · low · Duplicate header columns silently collapse (last wins): 'name,name\\nAlice,Bob' -> 'Bob'. · Detect duplicate headers and warn/dedup.
108. ingest/resume_parser.py:143-149 (blank country) · low · Cities outside the slice and not matching a country token return country='' (US-biased blank): 'San Francisco', 'New York' -> ''. · Maintain a city->country map.
109. io.py:11-13 (gzip case-sensitive) · low · Gzip detection uses suffix=='.gz' (exact, case-sensitive), so data.JSONL.GZ opens with plain text open() and feeds gzip bytes to json.loads. · Use suffix.lower()=='.gz' or check gzip magic bytes.
110. ingest/resume_parser.py:316-332 (single-char skill) · low · `if 2 <= len(name) <= 30` drops single-char skills C and R. · Whitelist single-char skills or lower the min with a stoplist.
111. ingest/resume_parser.py:48-50 (_DATE_RANGE separator) · low · The separator [-–to]+ is a character class not literal 'to', so '2019 ot 2021' / '2019 o 2021' over-accept. · Use proper alternation (?:\\s*(?:[-–]|to)\\s*).
112. ingest/resume_parser.py:386-387 (_parse_with_llm regex) · low · re.search(r'\\{.*\\}', DOTALL).group() can raise on None, the greedy match spans multiple objects, and content[0].text fails for non-text blocks — all swallowed by a broad except, silently degrading to local parse. · Use balanced/non-greedy JSON extraction and log fallbacks.
113. ingest/resume_parser.py:382-383 (max_tokens=2000) · low · Hardcoded max_tokens=2000 for a 12k-char prompt truncates long-resume JSON, defeating the LLM path exactly where it's most needed (degrades silently). · Raise max_tokens and detect stop_reason=='max_tokens'.
114. ingest/resume_parser.py:388-398 (LLM no validation) · low · LLM JSON is trusted with no normalization and confidence is hardcoded 0.9 regardless of completeness (the claimed crash is overstated). · Normalize LLM JSON to canonical shapes and set confidence from actual completeness.
115. ingest/model.py:135-148 (parse_duration_months month) · low · No validation of the month value; month 13/00/99 is accepted into arithmetic, corrupting duration. · Clamp/validate month to 1..12.
116. ingest/model.py:135-148 ('present'=2026-06) · low · 'present' is hardcoded to 2026-06, so as time advances every current-job duration is frozen/under-counted. · Use datetime.now() or an injectable as-of date.
117. ingest/resume_parser.py:99,131-132 (career fallback) · low · With no EXPERIENCE section, _parse_experience runs over all lines so contact/summary lines mis-parse as jobs, and confidence still credits bool(c.career), inflating it. · Require a date/company before emitting a job; don't credit synthesized career toward confidence.
118. ingest/adapters.py:78-80 (skills split) · low · Skills split only on [;,|], so newline/slash/'and'-separated cells produce one giant skill string. · Broaden the split set and trim.

## UI (18) — 1 critical · 3 high · 6 medium · 8 low

### critical
119. product_ui.py:152-164,122 · critical · Server/user strings (title, candidate_id, reasoning, location, requirement, matched_keywords, filename) are interpolated raw into innerHTML with no escaping; a resume summary or filename with markup executes in the recruiter browser (stored/reflected/DOM XSS). · Add an esc() helper and wrap every interpolated string before innerHTML.

### high
120. studio_ui.html:268 + product_ui.py:122 (renderFiles) · high · Uploaded file names are rendered raw into the file-chip list via innerHTML, bypassing esc(); a maliciously-named file renders before ranking (DOM XSS). · Wrap f.name in esc() in both add()/renderFiles().
121. studio.py:179 do_challenge + studio_ui.html:396 · high · do_challenge hardcodes valid:True and the UI shows a green 'VALID / official validator' stat, but no official validator is ever run on the live ranking. · Run the real validator and return its boolean, or relabel the stat.
122. studio.py:220-235 do_top10detail · high · do_top10detail re-reads candidates.jsonl (100K), re-ranks the full pool, and runs _old_substring_rank (json.dumps over all rows) on EVERY click with no cache, unlike do_challenge. · Cache by pool fingerprint or route through live_cache.

### medium
123. studio_ui.html:279 esc() · medium · esc() only replaces '<' and '"' — not '>', '&', or single-quote — so '&'/entity text renders wrong and any single-quoted attribute is injectable. · Escape all of & < > " ' (escape & first).
124. studio.py:228-231 vs live_cache.py:29-36 CHALLENGE_JD · medium · The 'What changed' tab ranks the 100K with a different JD string than the '100K challenge' tab, yet both advertise 'the real top 10', so they can diverge. · Use live_cache.CHALLENGE_JD as the single source of truth.
125. studio.py:76-78 + product_ui.py:212-213 · medium · Every parse-failed file is appended with the same hardcoded id 'CAND_0000000', and by_id collapses them into one (silent data loss; can also collide with a real id). · Generate a unique id per failed file.
126. studio.py:150-162 (spine empty requirement_matches) · medium · The spine engine returns empty requirement_matches while hybrid populates it; do_rank uses spine for >200 records, so large uploads silently lose the 'proof from your own words' quotes shown for small uploads. · Populate matched_requirements in the spine path or make the missing-evidence case explicit.
127. studio.py:42-55 _get_embedder + live_cache.py:133-136 · medium · _EMBEDDER lazy init is unprotected on a ThreadingHTTPServer, so two concurrent first requests each load the ~90MB model; live_cache reads the cache outside the lock (TOCTOU). · Guard init and the cache read/copy with a double-checked Lock.
128. studio.py:71-74 + product_ui.py:207-210 (tempfile leak) · medium · Binary uploads are written to NamedTemporaryFile(delete=False) and never removed, leaking a temp file per upload for the process lifetime. · try/finally os.unlink after ingest, or ingest from an in-memory buffer.
129. studio.py:305-306 + product_ui.py:260-261 + app.py:983-984 + api_server.py:184-186 · medium · No Content-Length cap in any of the four handlers: int(Content-Length) then rfile.read(length) then json.loads the whole body allows unbounded allocation; a non-numeric header also yields a 500 instead of 400 (memory-exhaustion DoS, --host configurable). · Reject bodies over a sane cap before reading; wrap the int() parse and return 400.

### low
130. studio.py:112-113,134-142 vs studio_ui.html:331-333 · low · fit_label is computed server-side from unrounded role_relevance while fitClass color uses the 2-decimal rounded value, so boundary values can disagree (cosmetic). · Compute fitClass from c.fit_label or send one fit tier.
131. studio.py:293-295 do_GET · low · do_GET calls INDEX_HTML.read_text() per request with no try/except (unlike do_POST), so a missing/locked file yields a broken response/traceback instead of a clean 500. · Wrap in try/except returning 500; cache file contents at startup.
132. app.py:931-944 download route · low · Content-Disposition is built as filename="{name}" where name can retain quotes/CRLF -> header-injection in principle (very low real exploitability given existence/path gating). · Strip quotes/CR/LF or RFC5987-encode the filename.
133. studio_ui.html:430-432 + studio.py:256 · low · delta=old-new classed as >=0?'up':'down' with text '(+0)', so a candidate with no rank change renders as green 'up' '(+0)'. · Add a delta===0 neutral 'unchanged' case.
134. studio.py:241,257 + studio_ui.html:430-431 · low · old=old_rank.get(cid, len(rows)) would show 'was #100000 (outside top 10)' for an absent candidate — a fabricated precise rank (does not actually manifest because every candidate is present in old_rank). · Distinguish 'not previously ranked' from a numeric old rank.
135. studio_ui.html:281-297 switchView · low · loadChallenge/loadWhatsNew never reset go.disabled, so switching tabs mid-rank can leave the Rank button stuck disabled; tab clicks also re-fire heavy backend computes with no in-flight guard. · Reset go.disabled in switchView and guard repeat loads.
136. studio.py:151-162 do_rank (engine threshold + top_n) · low · The engine silently switches hybrid->spine purely on record count (<=200), changing scoring and whether proofs appear, and top_n is hardcoded to min(50,len) ignoring any requested Top N. · Surface the engine/threshold decision and respect a requested top_n up to a documented cap.

## INTERFACES (21) — 1 critical · 2 high · 5 medium · 13 low

### critical
137. mcp_server.py:284-296 serve_stdio + handle:243-245 · critical · serve_stdio calls handle() with no guard, so a JSON line parsing to a non-dict (array/scalar) makes request.get('method') raise AttributeError, the exception is uncaught, and the whole server process dies, losing all later valid requests. · Wrap handle() in try/except and return -32600 for non-dict requests.

### high
138. mcp_server.py:191-198 + api_server.py:110-114 -> consistency_audit.audit_candidate:91-101 · high · A caller-supplied candidate is passed to audit_candidate with no normalization; a string candidate or {'profile':null} crashes (profile.get on None), surfacing as a REST 500. · Normalize the candidate (coerce null profile/lists, ingest strings) before auditing.
139. jd_ingest.py:233-293 ingest_text (single-line JD) · high · A short single-line JD ('Senior AI Engineer') yields ZERO requirements (the only line is consumed as the title), so rank() scores everyone against an empty must-have set and the ranking collapses to generic quality only — reachable from MCP ingest_jd, /ingest/jd, and every terse-JD rank(). · When the JD collapses to title-only, derive requirements from title tokens + category core_signals or surface a 'no extractable requirements' note.

### medium
140. api_server.py:184-191 do_POST exception handling · medium · Only KeyError maps to 400; every other handler exception is caught broadly and returned as 500 with the raw f'{type.__name__}: {exc}', so client-input errors get 500 and the raw exception type/message leaks. · Validate/normalize inputs, map validation failures to 400 with a generic message, and log details server-side.
141. api/facade.py:216-217 (negative top_n) · medium · Negative top_n is not validated: `len(ranked) > top_n` is true and ranked[:top_n] uses negative slicing, silently returning a wrong-sized, wrongly-truncated list (rank(...,top_n=-2) returned 4 of 8). · Clamp top_n = max(0, top_n) (or raise) before slicing.
142. api/facade.py:55-72 _resolve_job · medium · When the network-facing jd string is a path to an existing .txt/.md/.yaml/.yml file on the server, _resolve_job reads that file and uses its contents as the JD — an unintended local-file read driven by untrusted REST/MCP input. · Don't treat the network-facing jd as a filesystem path; gate path resolution behind an explicit flag/type.
143. client.py:27-39 _post/_get (SDK) · medium · urllib raises HTTPError on 4xx/5xx but the SDK never catches it or reads err.read(), so the server's JSON error body is discarded and the integrator gets a bare opaque HTTPError. · Wrap urlopen; on HTTPError read+decode err.read() and raise a typed exception with status + server message.
144. eval/compliance.py:127-148 compliance_summary · medium · overall_passes_four_fifths returns True vacuously when group_attributes is {} (the loop never runs), producing a green 'passes' compliance artifact with zero data behind it. · Treat empty group_attributes / no overlap as 'insufficient data', not a pass.

### low
145. api/facade.py:75-86 + types.py:14-23 + scoring.py:393-394 · low · Every requirement match view is hardcoded kind='must_have', score=0.0, dense=0.0, lexical=0.0; the upstream matched_requirements tuple drops kind/score, so the view can't carry them and any nice_to_have match is mislabeled must_have. · Carry the real kind and sub-scores into matched_requirements and the view.
146. api_server.py:36-48 OPENAPI vs ROUTES:137-144 · low · ROUTES exposes /candidate_report but OPENAPI omits it, and published paths carry only one-line summaries with no request/response schemas, so codegen against /openapi.json is incomplete. · Generate OPENAPI from the ROUTES table and include schemas.
147. jd_ingest.py:270 (i<4 title gate) · low · The title/intro skip only runs for the first 4 sentences, so a title appearing at index >=4 (after a multi-line company blurb) is scored as a must_have, re-introducing the title-inflates-weak-candidates bug. · Detect the title structurally regardless of position.
148. api/facade.py:42-43,179,206 (duplicate candidate_id) · low · _safe_records assigns a synthetic id only when the id is not a str, so duplicate candidate_ids pass through and the hybrid {cid:i} index collapses them, so a duplicate-id row gets the wrong embedding. · Detect duplicate candidate_ids in _safe_records and disambiguate/reject.
149. api/types.py:72 cross_encoder_score default · low · cross_encoder_score defaults to 0.0 and is only set in rerank, so on the non-rerank path it serializes as 0.0 for every candidate, indistinguishable from a real CE score of 0. · Default to None and populate only when rerank runs.
150. mcp_server.py:84-96,201-211 + api_server.py:127-134 · low · tool_candidate_report reads args.get('category') but the inputSchema only declares candidate+jd, so MCP clients can't document/set category (the 'REST crashes on string candidate' half is false). · Add 'category' to the MCP candidate_report inputSchema and coerce string candidates via a shared helper.
151. api_server.py:96-101 vs mcp_server.py:173-182 (/ingest/jd shape) · low · The two surfaces return different shapes for /ingest/jd: MCP includes 'keywords' per requirement, REST omits it, breaking 'one engine behind every surface'. · Share one requirement serializer across both surfaces.
152. api_server.py:165-180 (method handling) · low · A GET to a real POST route returns 404 instead of 405, and PUT/DELETE fall through to BaseHTTPRequestHandler's HTML 501, breaking JSON-only clients. · Return JSON 405 + Allow for valid paths; add do_PUT/do_DELETE emitting JSON 405.
153. api_server.py:159-163 _authorized · low · The API-key check uses plain `==` (not constant-time) and supports no Authorization/Bearer header. · Use hmac.compare_digest.
154. api_server.py:184-191 (no Content-Type check) · low · The server never validates Content-Type and blindly json.loads the body; a non-JSON body yields a JSONDecodeError caught broadly and returned as 500 instead of 400/415. · Return 400 'invalid JSON body' on parse failure; optionally require application/json (415 otherwise).
155. mcp_server.py:185-188 + api_server.py:104-107 (empty ingest) · low · When ingest produces no records, both return a bare {} with no error/status, so a caller can't tell 'empty profile' from 'unparseable resume' and MCP wraps {} as success. · Return {'error':'resume could not be parsed'} (or a status flag) when recs is empty.
156. api/batch.py:44-67 rank_many_jds · low · The docstring promises a JobSpec value 'is used as-is' with its own category, but the code always passes category=cat, so for a JobSpec value the categories override is silently ignored. · Honor the override for JobSpec values or document that they ignore categories.
157. api/batch.py:44-50,70-77 (engine default) · low · rank_to_csv/rank_many_jds default engine='spine' while facade.rank and REST/MCP default to hybrid, so the same JD+pool can produce a different order in the CSV vs the live API. · Align defaults from one place or document and surface the engine in the CSV.
158. mcp_server.py:163-170 (no engine arg) · low · tool_rank_candidates auto-picks hybrid vs spine on the 200-candidate threshold with no 'engine' arg in the schema, so an MCP client can't force an engine and 200-vs-201 candidates silently use different engines. · Add an optional 'engine' property to the rank_candidates schema and honor it.

## AUDIT / EVAL / TRUST (24) — 4 high · 9 medium · 11 low

### high
159. explanation_audit.py:30 · high · int(packet.get('rank',999)) raises ValueError on a non-numeric rank, aborting the audit of ALL packets (the auditor is meant to validate arbitrary/external JSONL). · Guard the int() parse and default to 999 on failure.
160. eval/compliance.py:109-111,140-143 · high · When no group is large enough, adverse_impact returns passes=True and compliance_summary ANDs it into overall_passes=True, so a legal/compliance export asserts 'passes four-fifths' for a test that was never validly run. · Use a tri-state (passes=None / not_assessed) and don't aggregate untested attributes as passing.
161. eval/fairness.py:61-78 · high · The name-invariance audit only mutates profile.anonymized_name, which build_evidence never reads, so the score can never move — the test is vacuous and its name_invariant=True / delta=0.0 claim is unearned, yet it underpins FCRA-style disclosures. · Swap the name into the fields the engine actually reads (headline/summary/current_title) or drop the numeric guarantee.
162. consistency_audit.py:114-122 · high · int(s.get('duration_months') or 0)==0 treats a MISSING duration as 0, so an 'expert' skill with no duration field triggers expert_zero_duration, a HARD top-rank veto — real ingested profiles routinely omit per-skill duration. · Only flag when duration_months is explicitly present AND equals 0.
163. consistency_audit.py:104-111 · high · tenure_exceeds_experience (HARD veto) naively sums duration_months across jobs with no overlap handling, so legitimately concurrent/overlapping roles sum past calendar experience and are falsely flagged impossible (reproduced with two overlapping 72-month jobs). · Compute calendar-distinct tenure (union of date ranges).

### medium
164. explanation_audit.py:78-84 · medium · No stemming: evidence 'embeddings' but reasoning 'embedding' fires a spurious 'unsupported term' grounding warning on grounded reasoning. · Normalize stems or add singular/plural variants to evidence_tokens.
165. eval/metrics.py:69-75 · medium · precision_at_k always divides by k even when the list is shorter than k, understating precision (P@10 of a length-8 list = 0.2 instead of 0.25). · Divide by min(k, len(relevances)).
166. eval/metrics.py:78-88 · medium · honeypot_rate_at_k divides by len(window) while precision_at_k divides by k — inconsistent denominators; on a short list honeypot rate overstates (5 zeros / k=100 reads as 1.0). · Pick one denominator convention to match the top-100 rule.
167. eval/compliance.py:113-124 · medium · A single eligible group with zero selections yields max_rate=1.0, impact 0.0, passes=False — flagging adverse impact where four-fifths is undefined (no comparison group). · If fewer than 2 eligible groups, return passes=True with a 'not applicable' note.
168. consistency_audit.py:138-147,192-197 · medium · Future END dates are never checked; date_integrity only tests _is_future(start_date), so end_date='2030-01-01' is undetected. · Also call _is_future on a non-null end_date.
169. eval/datasets.py:181-203 · medium · Generated honeypot/strong jobs set duration_months ~93 while start/end span only ~11 months; the auditor has no duration-vs-span check, so this per-job contradiction is invisible. · Add a duration-vs-span check and/or make the generator consistent.
170. eval/datasets.py:189-202 · medium · Career history is chronologically inverted in 'clean' STRONG records: the is_current job has the earliest start_date while non-current jobs start later — an impossible timeline in positive controls. · Order jobs so the is_current role has the most recent start_date.
171. candidate_report.py:48-55,94 + github_analysis.py:66-90 · medium · candidate_report calls analyze_github(), which makes a blocking outbound urllib fetch to api.github.com when a GitHub URL is present, while the disclosure asserts 'only the information you provided — no scraped or third-party data' (broad except hides failures, ~6s latency). · Gate the fetch behind explicit consent and update the disclosure, or remove it from the transparency path.
172. candidate_report.py:67-68 · medium · The disclosure hardcodes 'identity_used: none (name-swap score change = 0.0)' as an absolute per-candidate factual claim, asserted not measured, resting on the vacuous fairness audit (#161). · Compute the actual name-swap delta per candidate (after fixing #161) or soften to a structural statement.

### low
173. explanation_audit.py:34-37 · low · Empty reasoning emits BOTH 'empty reasoning' and 'reasoning too short' (no elif) — duplicate noise. · Use elif so 'too short' only fires for non-empty reasoning.
174. explanation_audit.py:23-26 · low · reason_counter includes empty reasoning strings, producing a meaningless 'repeated reasoning 2 times: ' (blank). · Skip blank reasonings in the repeat Counter.
175. eval/metrics.py:43-49 · low · NDCG returns 1.0 for all-negative relevances with no guard on negative grades (latent; no caller passes negatives). · Clamp/assert relevances >= 0.
176. consistency_audit.py:200-205 · low · _date_before validates the YYYY-MM-DD shape via regex then compares raw strings, and _is_future accepts invalid months (_is_future('2026-13-01')=True). · Validate month 1..12 / day 1..31 and compare parsed (y,m,d) tuples.
177. consistency_audit.py:68-75 · low · _evidence_tokens is defined but never called anywhere (dead code); the auditor uses _evidence_blob. · Remove it or wire it in intentionally.
178. eval/datasets.py:118-165 · low · Inactive candidates get github_activity_score=-1.0, an invalid negative sentinel that numeric consumers (mean/normalize) would treat as a real value (no such consumer found yet). · Use None/absent for unknown, or clamp at 0.
179. api/facade.py:75-86 · low · req_text, kws = item[0], item[1] assumes >=2-element tuples; a 1-element tuple raises IndexError (both real producers emit 3-tuples, so latent). · Guard: if len(item) < 2, skip or default kws=[].
180. risk_audit.py:24-33 · low · weights.get(flag, 0.05) gives any unknown/misspelled flag a silent 0.05 penalty instead of ignoring it (latent — all current flags are in the map). · Default unknown flags to 0.0; only sum known weights.
181. risk_audit.py:17 · low · stale_low_response treats response_rate 0.0 from 'never contacted' identically to genuine unresponsiveness, penalizing newcomers with no outreach history (partly mitigated by the 6-month gate). · Distinguish 'no outreach history' from 'low response'.
182. eval/metrics.py:52-66 · low · The composite labels the 0.15 term 'MAP' but average_precision computes AP over a single full list with no @k cutoff and never averages across queries, inconsistent with the NDCG@10/@50 terms. · Rename to AP for a single list, compute true MAP across the JD set, document the @k window.